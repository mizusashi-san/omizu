import { account, session, user, verification } from './auth-schema';
import { userGoals, waterIntakes } from './water-schema';

export const schema = {
  user,
  session,
  account,
  verification,
  waterIntakes,
  userGoals,
};
